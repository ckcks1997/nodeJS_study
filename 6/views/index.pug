doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        style.
            h1{
                font-size: 20px;
            }
    body 
        #login-button
            input(placeholder=title+': input here..')
            button(type='submit') 전송

        .post-image
            h1 welcome to #{title}
        
        - const node = 'nodeJS';  
        p #{node}

        p!= '<> not escaped'

        - const arr1 = ['a', 'b', 'c'];

        ul
            each fruit in #{arr1}
            li= fruit

        script.
            const message = 'hello';
            //alert(message);
